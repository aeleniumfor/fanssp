version: "3"
services:
    mock_dsp:
        build: ./mock/
        restart: always
    
    ssp:
        build: ./ssp/
        restart: always
        ports:
            - 8888:8081
        env_file:
            - host.env